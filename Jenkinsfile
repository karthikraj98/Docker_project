pipeline {
    agent any 

    stages {
        stage('Hello') {
            steps {
                echo 'Hello'
            }
        }

        stage('Welcome') {
            steps {
                echo 'Welcome to jenkins'
            }
        }

        stage('introduction') {
            steps {
                echo '''
                today we i am here writing my firstpipeline with verify 
                my previous written pipelines.After writting this pipeline 
                i will build the job to check whether the pipeline is working are not
                '''
            }
        }
    }
}